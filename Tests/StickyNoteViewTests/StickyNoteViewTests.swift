    import XCTest
    @testable import StickyNoteView

    final class StickyNoteViewTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(StickyNoteView().text, "Hello, World!")
        }
    }
